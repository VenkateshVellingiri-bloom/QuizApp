const initialState = {
    questions: [{ id:1 , question: 'What is your favourite programming language ?'}]
}

const questions = (state = initialState, action) => {
    return state;
}

export default questions;